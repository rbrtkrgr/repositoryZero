name: Create Pre-release

on:
  workflow_dispatch:
  #push:
  #push:

env:
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  
  #REPO: "repositoryZero"
defaults:
  run:
    shell: bash

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Create prerelease
        id: create-release
        uses: actions/create-release@v1
        with:
          tag_name: 1.1.0-beta${{github.run_number}}
          release_name: 1.1.0-beta${{github.run_number}}
          draft: true
          prerelease: true
      
